import org.scalatest.FunSuite

class ReaderTest extends FunSuite {
  test("Reader.stringToDate") {
    assert(1 === 0)
  }

  test("Reader.readFlightRecord") {
    assert(1 === 0)
  }

  test("Reader.readPassengerRecord") {
    assert(1 === 0)
  }
}
